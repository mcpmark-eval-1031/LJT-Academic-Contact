# The following is the order of the links in the header of the website.
#
# This repository is maintained as a simple academic contact card, so the
# header only links back to the main About page.

main:
  - title: "About"
    url: /
